# How to start the peer
To simplify it to the point that it can work on its own, the known hosts are presumed to be on the same system (that is, 127.0.0.1) and just on different ports
With no arguments, it just starts up on port 8470.
Otherwise the first argument is treated as the port to run on.
Any following arguments are treated as ports with known hosts.
